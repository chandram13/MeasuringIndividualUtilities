#include <stdio.h>
#include <assert.h>

// Marvish Chandra
// Measuring Individual Utilities

static void differingUtilities(uA,sA,xA,uB,sB,xB,uC,xC,sC){
    assert = xA + 1/2 * sA; // biocentric
    assert = xB + uA; // alutristic
    assert = xC + sA + sB + sC; // paternalistic
    return 0;
}